version: "2"
sql:
  - schema: "scripts/init.sql"
    queries: "db/query/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: true
